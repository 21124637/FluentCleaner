<Window
    x:Class="FluentCleaner.Views.RuleLabWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- custom title bar:app icon + title -->
        <TitleBar x:Name="AppTitleBar" Grid.Row="0" Title="Rule Lab">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <!-- content -->
        <Grid Grid.Row="1" Padding="20,8,20,20" RowSpacing="12" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- header: Dry Run + spinner + summary -->
            <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal"
                        Spacing="10" VerticalAlignment="Center">
                <Button x:Name="DryRunButton" Click="DryRun_Click" />
                <ProgressRing x:Name="Ring" Width="18" Height="18" IsActive="False" />
                <TextBlock x:Name="Summary" FontSize="12" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- left: editable rules -->
            <TextBox x:Name="SourceBox" Grid.Row="1" Grid.Column="0"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     FontFamily="Consolas" FontSize="12"
                     IsSpellCheckEnabled="False"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" />

            <!-- right: dry-run log -->
            <TextBox x:Name="ResultsBox" Grid.Row="1" Grid.Column="1"
                     IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                     FontFamily="Consolas" FontSize="11"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" />

            <!-- bottom bar: Copy / Report / Close -->
            <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal"
                        Spacing="8" HorizontalAlignment="Right">
                <Button x:Name="CopyButton"   Click="Copy_Click" />
                <Button x:Name="ReportButton" Click="Report_Click" />
                <Button x:Name="CloseButton"  Click="Close_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
